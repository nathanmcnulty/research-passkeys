# Azure Automation samples

These samples show how Azure Automation can orchestrate the repo's passkey Function endpoints without re-embedding the full registration or login logic inside a runbook.

Current samples:

- `function-passkey-runbooks`: PowerShell runbooks that call the PowerShell or Python Function endpoints for TAP registration, ESTSAUTH registration, and passkey login
