# windows-passkey-provider

This folder is for Windows passkey provider work, including .NET host experiments, native experiments, packaging notes, and provider-specific research.

Provider-host complexity should stay explicit here. Only the reusable pieces should be promoted into `shared/` and `contracts/`.
