# shared

This folder is for host-agnostic contracts, design notes, and reusable implementation assets that should be consumed by more than one track.

Examples:

- auth abstractions
- Key Vault helpers
- metadata protection logic
- WebAuthn helper logic

If a piece of code is only meaningful inside one host or experiment, keep it in that track folder first.
