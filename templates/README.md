# templates

Templates are the publishable starting points that downstream repos should be able to adopt with minimal reshaping.

Current templates:

- `function-app\powershell-keyvault-passkey-http`
- `function-app\python-keyvault-passkey-http`

Planned next templates:

- browser-extension starter
- Windows passkey provider starter
