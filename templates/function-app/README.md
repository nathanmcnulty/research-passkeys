# Function App templates

These templates are publishable starter shapes derived from the validated Function App samples in `function-app\`.

Current starters:

- `powershell-keyvault-passkey-http`
- `python-keyvault-passkey-http`

Use `scripts\packaging\Export-FunctionTemplate.ps1` to copy a starter into a downstream repo.
