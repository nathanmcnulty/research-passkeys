# Logic App templates

Current Logic App templates:

- `passkey-function-http`: Consumption workflows that proxy request payloads into the passkey Function endpoints for TAP registration, ESTSAUTH registration, and passkey login
